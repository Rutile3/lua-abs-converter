# 変換仕様

このツールは、Luaソースコード中の `abs(変数)` を含む比較式を検出し、選択された変換ルールに従って等価な式へ置換する。

## 実装方針

Luaソース全体を構文解析するのではなく、正規表現で限定された形の比較式を検出して置換する。

現在の変換対象は次の2種類。

```lua
abs(x) == n
abs(x) <= n
```

検出時には、次の3要素を取り出す。

```text
abs(変数) 演算子 比較対象
```

例:

```lua
abs(x) <= 3
```

```text
変数: x
演算子: <=
比較対象: 3
```

## 対応する形式

左辺の `abs()` 内は、単一の識別子に対応する。

```lua
abs(x)
abs(foo)
abs(value_1)
```

比較対象は、次の形式に対応する。

```lua
3
3.5
N
(A+B)
```

つまり、数値・識別子・改行を含まない単純な括弧式に対応する。

## 変換ルール

### イコール（==）

`abs(x) == n` は、選択されたルールに応じて次のように変換する。

| ルール | 出力 |
| --- | --- |
| `abs` | `abs(x) == n` |
| `square` | `x^2 == n^2` |
| `split` | `(x == -n or x == n)` |

比較対象が数値の場合、二乗変換では実際に二乗した値を出力する。
比較対象が識別子以外の式の場合は、必要に応じて括弧で包んで出力する。すでに単純な括弧式になっている場合は、追加の括弧は付けない。

```lua
abs(x) == 4
```

```lua
x^2 == 16
```

### 小なりイコール（<=）

`abs(x) <= n` は、選択されたルールに応じて次のように変換する。

| ルール | 出力 |
| --- | --- |
| `abs` | `abs(x) <= n` |
| `square` | `x^2 <= n^2` |
| `range` | `(-n <= x and x <= n)` |

比較対象が数値の場合、二乗変換では実際に二乗した値を出力する。

```lua
abs(x) <= 3
```

```lua
x^2 <= 9
```

## 制限事項

現在の実装は正規表現による限定的な変換であり、Luaの完全な構文解析は行わない。

そのため、次のようなケースは非対応または誤変換の可能性がある。

- コメント内の `abs(x) == n`
- 文字列リテラル内の `abs(x) <= n`
- `abs(a + b)` のように `abs()` 内が単一識別子ではない式
- `A + B` のように括弧で囲まれていない複合式
- `((A+B))` のような入れ子の括弧式
- `foo.bar` や `tbl[i]` のようなLuaの複合的な参照
- `n == abs(x)` や `n >= abs(x)` のような左右反転した比較式

これらを正確に扱う場合は、正規表現ではなく、コメント・文字列を除外する字句解析またはLua構文に近い解析処理が必要になる。
